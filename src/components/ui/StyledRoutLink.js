import { Link } from "react-router-dom";
import styled from "styled-components";

export const StyledRoutLink = styled(Link)`
  text-decoration: inherit;
  color: inherit;
  cursor: inherit;

  &:hover {
    text-decoration: inherit;
    color: inherit;
    cursor: inherit;
  }

  &:active {
    text-decoration: inherit;
    color: inherit;
    cursor: inherit;
  }
`;
